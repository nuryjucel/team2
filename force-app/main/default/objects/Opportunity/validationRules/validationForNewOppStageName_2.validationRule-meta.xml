<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>validationForNewOppStageName_2</fullName>
    <active>true</active>
    <errorConditionFormula>ISNEW() &amp;&amp;   (ISPICKVAL( StageName, &apos;Closed Won&apos;)  ||   ISPICKVAL( StageName , &apos;Closed Lost&apos;) )</errorConditionFormula>
    <errorDisplayField>StageName</errorDisplayField>
    <errorMessage>Yeni oluşturulan Opportunity kaydında stage Closed Won veya Closed Lost olamaz (Stage cannot be Closed Won or Closed Lost in newly created Opportunity record )</errorMessage>
</ValidationRule>
